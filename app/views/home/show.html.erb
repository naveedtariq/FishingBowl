<div id="topBar">
	<h1>FishingBowl</h1>
	<h3 class="Login" onClick="$('a.loginbox').trigger('click');">Sign in</h3>
	<% if current_user %>
		<h3 class="Login" onClick="$('a.invitebox').trigger('click');">Invite Friends</h3>
		<a href="logout"><h3 class="Login">Logout</h3></a>
	<% end %>
</div>

<a href="/loginbox.html" class="loginbox"></a>
<a href="/invitebox.html" class="invitebox"></a>

<div id="photos"></div>

<div id="camera">
	<span class="tooltip"></span>
	<span class="camTop"></span>

	<div id="screen"></div>
	<div id="buttons">
		<div class="buttonPane">
		<a id="shootButton" href="" class="blueButton">Shoot!</a>
		</div>
		<div class="buttonPane hidden">
		<a id="cancelButton" href="" class="blueButton">Cancel</a> <a id="uploadButton" href="" class="greenButton">Upload!</a>
		</div>
	</div>

	<span class="settings"></span>
</div>


<div>
	<% form_for :photo, @photo, :url => photos_path, :html => { :multipart => true } do |form| %>
		<%= form.file_field :picture %>
		<%= form.submit %>
	<% end %>
</div>
